import javax.swing.*;
import java.awt.*;

public class GridLayoutDemo extends JFrame{
    private JPanel JPanel;
    private JFrame JFrame;
    private JList<String>layoutList;
    private GridLayout GridLayout;
    public GridLayoutDemo(){
       
    
        JFrame frame=new JFrame();
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setLayout();
        Container contentpane=getContentPane();
        container.setLayout(new GridLayout(3,2,10,10));
        container.add(new container(Button1));
        container.add(new container(Button2));
        container.add(new container(Button3));
        container.add(new container(Button4));
        container.add(new container(Button5));
        panel=new panel();
        panel.setLayout(new GridLayoutDemo());
        add(panel,GridLayout.CENTER);
        for(int i=1;i<=5;i++)
        {
             panel.add(new JButton("Box " + i), GridLayout.CENTER);
        }
        layoutList.addListSelectionListener(e -> {
            if (!e.getValueIsAdjusting()) {
                updateLayout(layoutList.getSelectedValue());
            }
        });
         updateLayout(layouts[0]);

        frame.pack();
        frame.setVisible(true);
          
       

       
    }

          public static void main(String args[]){
            new GridLayoutDemo();
          }
       
        }


  
        


    
    


        